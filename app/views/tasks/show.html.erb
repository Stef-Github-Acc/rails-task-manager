<h2><%= @tasks.title %></h2>
<p><%= @tasks.details %></p>

<%= link_to 'Back to all tasks', tasks_path %>
<%= link_to 'Delete',
  task_path(@tasks),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
